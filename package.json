{
  "name": "yuser-network-subquery",
  "version": "1.0.0",
  "description": "a subquery project for handling yuser-network on-chain events",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/yusernetwork/yuser-subquery",
  "repository": "github:yusernetwork/yuser-subquery",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "devDependencies": {
    "@polkadot/api": "^7.9.1",
    "@polkadot/api-augment": "^7.9.1",
    "@subql/cli": "^0.15.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@subql/contract-processors": "^0.5.1",
    "@subql/types": "^0.10.0",
    "ethers": "^5.5.0"
  }
}
